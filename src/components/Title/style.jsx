import { StyleSheet } from 'react-native'

const Styles = StyleSheet.create({
      boxTitle: {
            alignItems: 'center',
            justifyContent: 'center',
            padding:10,
            flexDirection:'row'
      },

      textTitle: {
            color: 'blue',
            fontSize: 30,
            fontWeight: 'bold',
            fontFamily: 'JosefinSans_400Regular', // Aplica a fonte
      }
})

export default Styles;